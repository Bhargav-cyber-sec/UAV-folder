# Target: Python 3.10, win_amd64 (confirmed installed on the facility PC —
# do not change without re-confirming on-site)
# Phase 1 — core deps only. More added per phase (opencv/ultralytics in Phase 3,
# faster-whisper in Phase 6, streamlit/reportlab in Phase 9, etc.)
# CPU-only, confirmed — no CUDA toolkit is installed on the facility PC. All
# torch/GPU-capable packages added in later phases must use CPU wheels; this
# is not a fallback, it's the actual environment.

pyyaml>=6.0
python-dotenv>=1.0
click>=8.1
